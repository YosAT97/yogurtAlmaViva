<!DOCTYPE html> <html lang="es"> <head> <meta charset="UTF-8"> <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0"> <title>🥛 YogurtRutas Pro</title> <script src="https://cdnjs.cloudflare.com/ajax/libs/react/18.2.0/umd/react.production.min.js"></script> <script src="https://cdnjs.cloudflare.com/ajax/libs/react-dom/18.2.0/umd/react-dom.production.min.js"></script> <script src="https://cdnjs.cloudflare.com/ajax/libs/babel-standalone/7.23.5/babel.min.js"></script> <style> * { box-sizing: border-box; margin: 0; padding: 0; -webkit-tap-highlight-color: transparent; } body { background: #f0ede8; font-family: Georgia, serif; color: #2d2d2d; } </style> </head> <body> <div id="root"></div> <script type="text/babel"> const { useState, useMemo, useEffect } = React; const YOGURT_PRODUCTS = [ { id: "medio", name: "Yogurt ½L", emoji: "🥛" }, { id: "litro", name: "Yogurt 1L", emoji: "🍶" }, ]; const FRECUENCIAS = ["Diario","Lunes/Miércoles/Viernes","Martes/Jueves","Semanal","Quincenal"]; const ZONAS = ["Centro","Norte","Sur","Este","Oeste"]; const DEMO_CLIENTS = [ { id: 1, nombre: "María González", telefono: "555-1234", direccion: "Calle Rosal 45, Col. Centro", zona: "Centro", frecuencia: "Lunes/Miércoles/Viernes", productos: [{id:"litro",qty:2}], pagos: [{fecha:"2024-02-01",monto:180,metodo:"Efectivo",nota:""}], activo: true, notas: "Entregar temprano" } ]; function loadClients() { try { const saved = localStorage.getItem("yogurt_clients"); return saved ? JSON.parse(saved) : DEMO_CLIENTS; } catch(e) { return DEMO_CLIENTS; } } function saveClients(clients) { try { localStorage.setItem("yogurt_clients", JSON.stringify(clients)); } catch(e) {} } const emptyClient = () => ({ id: null, nombre: "", telefono: "", direccion: "", zona: "Centro", frecuencia: "Semanal", productos: [{id:"litro",qty:1}], pagos: [], activo: true, notas: "", }); // --- HELPERS --- const getDaysSinceLastPago = (pagos) => { if (!pagos || pagos.length === 0) return Infinity; const lastDate = new Date(pagos[0].fecha); const diffTime = Math.abs(new Date() - lastDate); return Math.ceil(diffTime / (1000 * 60 * 60 * 24)); }; // --- COMPONENTES UI --- function StatCard({ emoji, label, value, color }) { return ( <div style={{background:color, borderRadius:14, padding:16, textAlign:'center'}}> <div style={{fontSize:26, marginBottom:6}}>{emoji}</div> <div style={{fontSize:22, fontWeight:700}}>{value}</div> <div style={{fontSize:12, color:'#666'}}>{label}</div> </div> ); } function App() { const [clients, setClients] = useState(loadClients); const [view, setView] = useState("lista"); const [selectedClient, setSelectedClient] = useState(null); const [search, setSearch] = useState(""); const [editMode, setEditMode] = useState(false); const [editData, setEditData] = useState(null); const [pagoModal, setPagoModal] = useState(false); const [montoPago, setMontoPago] = useState(""); useEffect(() => { saveClients(clients); }, [clients]); const filtered = useMemo(() => clients.filter(c => c.nombre.toLowerCase().includes(search.toLowerCase()) || c.direccion.toLowerCase().includes(search.toLowerCase()) ), [clients, search]); const registrarPago = () => { if (!montoPago) return; const pago = { fecha: new Date().toISOString().split('T')[0], monto: montoPago, metodo: "Efectivo" }; const updated = { ...selectedClient, pagos: [pago, ...selectedClient.pagos] }; setClients(clients.map(c => c.id === updated.id ? updated : c)); setSelectedClient(updated); setPagoModal(false); setMontoPago(""); }; // VISTA LISTA if (view === "lista") { return ( <div style={S.page}> <div style={S.header}> <h1 style={{fontSize:22}}>🥛 YogurtRutas</h1> <button style={S.iconBtn} onClick={() => setView("stats")}>📊</button> </div> <div style={{padding:16}}> <input style={S.input} placeholder="Buscar cliente..." value={search} onChange={e => setSearch(e.target.value)} /> {filtered.map(c => { const dias = getDaysSinceLastPago(c.pagos); const pend = dias > 30; return ( <div key={c.id} style={S.card} onClick={() => {setSelectedClient(c); setView("detalle")}}> <div style={{flex:1}}> <div style={{fontWeight:700}}>{c.nombre} {pend && <span title="Pendiente de cobro">🔔</span>}</div> <div style={{fontSize:13, color:'#888'}}>{c.direccion}</div> </div> {pend && <div style={{color:'red', fontSize:12, fontWeight:'bold'}}>¡COBRAR!</div>} <div style={{marginLeft:10, color:'#ccc'}}>›</div> </div> ); })} </div> <button style={S.fab} onClick={() => { setEditData(emptyClient()); setEditMode(true); setView("edit") }}>+ Nuevo Cliente</button> </div> ); } // VISTA DETALLE if (view === "detalle" && selectedClient) { const dias = getDaysSinceLastPago(selectedClient.pagos); return ( <div style={S.page}> <div style={S.header}> <button style={S.backBtn} onClick={() => setView("lista")}>← Volver</button> <h2>Detalle</h2> <div/> </div> <div style={{padding:16}}> <div style={{...S.card, background: dias > 30 ? '#fff0f0' : '#f0fff0'}}> <strong>Estado de Cobro:</strong> {dias === Infinity ? " Sin pagos" : ` Último pago hace ${dias} días`} {dias > 30 && <div style={{color:'red', fontWeight:'bold'}}>⚠️ Requiere atención de cobro</div>} </div> <h3>{selectedClient.nombre}</h3> <p>📍 {selectedClient.direccion}</p> <p>📞 {selectedClient.telefono}</p> <hr style={{margin:'15px 0', border:'0.5px solid #ddd'}}/> <button style={S.saveBtn} onClick={() => setPagoModal(true)}>Registrar Pago $</button> {pagoModal && ( <div style={S.modal}> <div style={S.modalContent}> <h4>Monto a pagar:</h4> <input type="number" style={S.input} value={montoPago} onChange={e => setMontoPago(e.target.value)} /> <button style={S.saveBtn} onClick={registrarPago}>Confirmar</button> <button style={S.backBtn} onClick={() => setPagoModal(false)}>Cancelar</button> </div> </div> )} </div> </div> ); } // VISTA STATS if (view === "stats") { const pendientes = clients.filter(c => getDaysSinceLastPago(c.pagos) > 30).length; return ( <div style={S.page}> <div style={S.header}><button style={S.backBtn} onClick={() => setView("lista")}>← Volver</button><h2>Resumen</h2><div/></div> <div style={{display:'grid', gridTemplateColumns:'1fr 1fr', gap:10, padding:16}}> <StatCard emoji="👥" label="Total Clientes" value={clients.length} color="#e3f2fd"/> <StatCard emoji="🔔" label="Pendientes Cobro" value={pendientes} color="#ffebee"/> </div> </div> ); } return null; } const S = { page: { maxWidth: 480, margin: '0 auto', minHeight: '100vh', background: '#fafaf8' }, header: { display: 'flex', alignItems: 'center', justifyContent: 'space-between', padding: '15px', background: '#fff', borderBottom: '1px solid #ddd' }, card: { background: '#fff', padding: '15px', borderRadius: '12px', marginBottom: '10px', display: 'flex', alignItems: 'center', border: '1px solid #eee', cursor: 'pointer' }, input: { width: '100%', padding: '12px', borderRadius: '8px', border: '1px solid #ccc', marginBottom: '15px' }, fab: { position: 'fixed', bottom: 20, left: '50%', transform: 'translateX(-50%)', background: '#4a7c2f', color: '#fff', border: 'none', padding: '15px 25px', borderRadius: '30px', fontWeight: 'bold', boxShadow: '0 4px 10px rgba(0,0,0,0.2)' }, iconBtn: { background: '#eee', border: 'none', padding: '8px', borderRadius: '8px' }, backBtn: { background: 'none', border: 'none', color: '#4a7c2f', fontWeight: 'bold' }, saveBtn: { background: '#4a7c2f', color: '#fff', border: 'none', padding: '10px 20px', borderRadius: '8px', width: '100%', marginBottom: '10px' }, modal: { position: 'fixed', inset: 0, background: 'rgba(0,0,0,0.5)', display: 'flex', alignItems: 'center', justifyContent: 'center', padding: '20px' }, modalContent: { background: '#fff', padding: '20px', borderRadius: '15px', width: '100%' } }; ReactDOM.render(<App/>, document.getElementById("root")); </script> </body> </html>